{
  "name": "bookeater-cloudflare-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute bookeater-feedback --file=schema.sql --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "wrangler": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
